name: Deploy NonProd

on:
  push:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: nonprod
    permissions:
      id-token: write
      contents: read
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: (NonProd) Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ap-southeast-2

      - name: (NonProd) Dry Run Deployment to Validate Files
        run: |
          # TODO: Remove DATA_TAG once latest data is available in s3
          DATA_TAG=pr-7; 
          ./scripts/deploy.sh --project "$PWD/map-series/" --tag ${{ github.sha }} --source s3://linz-topography-nonprod/topographic-data/catalog.json --data-tag $DATA_TAG --target s3://linz-topography-nonprod/qgis/

      - name: (NonProd) Upload QGIS Projects to S3
        if: github.ref == 'refs/heads/master' && github.event_name == 'push'
        run: |
          # TODO: Remove DATA_TAG once latest data is available in s3
          DATA_TAG=pr-7; 
          ./scripts/deploy.sh --project "$PWD/map-series/" --tag ${{ github.sha }} --source s3://linz-topography-nonprod/topographic-data/catalog.json --data-tag $DATA_TAG  --target s3://linz-topography-nonprod/qgis/ --commit
          ./scripts/deploy.sh --project "$PWD/map-series/" --tag latest --githash ${{ github.sha }} --source s3://linz-topography-nonprod/topographic-data/catalog.json --data-tag $DATA_TAG  --target s3://linz-topography-nonprod/qgis/ --commit
